workflows:
  android:
    name: Build Android
    max_build_duration: 10
    environment:
      flutter: stable
    scripts:
      - flutter pub get
      - flutter build appbundle --debug
    artifacts:
      - build/app/outputs/flutter-apk/app-release.apk
